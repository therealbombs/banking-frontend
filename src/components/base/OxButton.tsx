import { Button, ButtonProps } from '@mui/material';
import { styled } from '@mui/material/styles';

export const OxButton = styled(Button)<ButtonProps>(() => ({
  // Stile personalizzato per il brand
}));