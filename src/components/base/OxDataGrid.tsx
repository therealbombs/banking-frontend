import { DataGrid, DataGridProps } from '@mui/x-data-grid';
import { styled } from '@mui/material/styles';

export const OxDataGrid = styled(DataGrid)<DataGridProps>(() => ({
  // Stile personalizzato per il brand
}));